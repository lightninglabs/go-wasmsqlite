version: "2"
sql:
  - engine: sqlite
    schema: "schema.sql"
    queries: "queries.sql"
    gen:
      go:
        out: "generated"
        package: "database"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_empty_slices: true